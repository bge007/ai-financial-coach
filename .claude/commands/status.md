Give me a status report on the MoneyMitra project:

1. Read `docs/ROADMAP.md` and report which phases are checked off.
2. Run `pytest backend/tests -q` and summarize pass/fail counts.
3. Scan `backend/app/` for any TODO/FIXME comments and list them by file.
4. Check whether `config/tax_fy*.yaml` matches the current financial year
   (today's date) and flag if it looks stale.
5. Tell me the single next recommended phase command to run.

Do not make any code changes — this is a read-only report.
